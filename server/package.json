{
  "name": "yt-comments-server",
  "version": "1.0.0",
  "description": "Backend server for YouTube Comments Extension",
  "main": "index.js",
  "scripts": {
    "build": "prisma generate && prisma db push --accept-data-loss",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.19.1"
  },
  "devDependencies": {
    "dotenv": "^17.2.3"
  }
}